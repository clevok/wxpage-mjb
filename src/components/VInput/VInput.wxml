<view class="input-parent">
    <input class="input" type="{{type}}" value="{{value}}" placeholder="{{placeholder}}" focus="{{focus}}"
        bindinput="bindinput" bindblur="bindblur" bindconfirm="bindconfirm" />
</view>