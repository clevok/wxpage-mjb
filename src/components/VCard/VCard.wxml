<view class="v-card">
    <view class="v-card-img">
        <image src="{{src}}" mode="aspectFill"></image>
    </view>
    <view>
        <view class="v-card-tips"><slot name="tips"></slot></view>
        <view class="v-card-title">{{title}}</view>
        <slot></slot>
    </view>
</view>
