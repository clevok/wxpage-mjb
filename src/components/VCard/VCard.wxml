<view class="v-card">
    <view class="v-card-img" style="background-image:url('{{src}}')">
        <view class="tips" wx:if="{{num}}">num</view>
    </view>
    <view>
        <view class="v-card-title"><slot name="title"></slot></view>

        <slot></slot>
    </view>
</view>
